/* 11 Sep 2021 12:15

*/
//:
// \sapporo +++> { | ... args |
// 	"hello ".post;
// 	args.postln;
// }
//:
\sapporo.fox({ | x |
	postf("received x value: %\n", x)
});
//:
\sapporo.fox({ | x |
	postf("received x value: %, mapping it to: %\n",
		x,
		x + 0.42 * 10
	)
});
//:

\sapporo.foy({ | x |
	postf("received y value: %\n", x)
});
//:

\sapporo.foz({ | x |
	postf("received z value: %\n", x)
});
//:
// \x.removeNotifier(IdeFantasy, '/sapporo');
// \y.removeNotifier(IdeFantasy, '/sapporo');
// \z.removeNotifier(IdeFantasy, '/sapporo');
\sapporo ---> \z;
\sapporo ---> \y;
// \sapporo ---> \x;
//
//: